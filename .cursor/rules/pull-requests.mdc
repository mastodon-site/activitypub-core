---
description: PR conventions including go fmt and semver labels
alwaysApply: true
---

# Pull Requests

## Before raising a PR (Go)

From the repository root (the directory containing `go.mod`), run formatting for the whole module:

```bash
go fmt ./...
```

This step is **required** before opening or updating a pull request. If `go fmt` changes any files, include those changes in the same commit or branch as the rest of the PR. Do not open a PR with unformatted Go source.

## Semver label

When creating a pull request with `gh pr create`, always add a semver label:

- **patch** — bug fixes, typos, config corrections, non-functional changes
- **minor** — new features, endpoints, or backwards-compatible enhancements
- **major** — breaking changes to APIs, database schemas requiring migration, or removed functionality

Use `--label <label>` on the `gh pr create` command. Pick exactly one of `patch`, `minor`, or `major`.

```bash
gh pr create --title "..." --body "..." --label minor
```
